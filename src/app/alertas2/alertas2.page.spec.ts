import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Alertas2Page } from './alertas2.page';

describe('Alertas2Page', () => {
  let component: Alertas2Page;
  let fixture: ComponentFixture<Alertas2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Alertas2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
